'use client'

import { useSearchParams } from 'next/navigation'

export default function SuccessContent() {
  const searchParams = useSearchParams()
  
  // Your existing success page logic here
  return (
    <div>
      {/* Your success page content */}
    </div>
  )
} 