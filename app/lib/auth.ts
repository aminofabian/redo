import { NextAuthOptions } from "next-auth";
// Import your auth providers and configuration

export const authOptions: NextAuthOptions = {
  // Your auth configuration
  // providers, callbacks, etc.
}; 